[mysqld]
# =============================================
#  MASTER - ASYNCHRONOUS (Default)
#  Master commit langsung tanpa tunggu slave
# =============================================

server-id         = 1
log_bin           = mysql-bin
binlog_format     = ROW
binlog_do_db      = demo_db
expire_logs_days  = 7
